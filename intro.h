// Handles the splash screen and logo sequences.
#pragma once

// Initializes the Intro state by loading logos and fonts.
void Intro_Init(void);

// Updates the Intro state, handling fading logic and timers.
void Intro_Update(void);

// Cleans up Intro state resources.
void Intro_Exit(void);