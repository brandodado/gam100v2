#pragma once

// These are the three main functions for the shop game state
// They will be called by CP_Engine_SetNextGameState()

void Shop_Init(void);
void Shop_Update(void);
void Shop_Exit(void);