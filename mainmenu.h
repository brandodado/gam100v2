// Handles the Main Menu state, including button rendering and state transitions.
#pragma once

// Initializes the Main Menu state, loading fonts and calculating button positions.
void Main_Menu_Init(void);

// Updates the Main Menu state, handling mouse input, button hovering, and clicking.
void Main_Menu_Update(void);

// Cleans up Main Menu resources (unused in this implementation but kept for structure).
void Main_Menu_Exit(void);